# Codex Progress

## 任务目标

按推荐顺序完成 OA 权限体系加固，包括角色管理、授权边界、ABAC/数据范围、对象级权限、JIT 四眼审批、测试与文档，最后执行 Docker 完整无缓存冷构建并重新部署验证。

## 已完成

- 已实现角色新增、编辑、启停、权限分配和继承关系管理。
- 已实现授权主体/范围校验、CUSTOM 组织范围、重复授权幂等、直接自授权拦截及高权限授权保护。
- 已修复 JIT 权限提升语义：仅临时增加权限，不覆盖长期 RBAC/ABAC/数据范围；新增申请、审批、拒绝四眼流程。
- 已实现 ABAC 按实际通过条件的来源角色收敛数据范围，修复多角色范围合并语义。
- 已为员工、流程、文档、资产、会议室、访客、公告、文件等业务补充数据范围或对象级访问控制。
- 已将多处业务查询从裸 JDBC 迁移到受数据权限插件治理的 MyBatis Mapper。
- 已补充缓存可靠失效、委托范围校验、公告受众 ACL 等安全加固。
- 已增加多组单元测试、架构守卫测试、数据库迁移和交付文档骨架。
- 已补齐 JIT 审批前端入口、公告 ACL、组织变更缓存测试、OpenAPI/权限常量和 CI compose 门禁。
- 已通过后端 `mvn clean package`、14 模块聚合测试、前端 109 tests、生产构建和体积预算。
- 已完成 4 个后端、PC、移动端 `--pull --no-cache` 冷镜像构建，并重建部署 6 个应用服务。
- 已确认共 10 个容器 healthy，Flyway V17/V83、角色管理、JIT 列表、401、OpenAPI 和服务 ping 冒烟通过。
- 已在运行态发现并修复提权列表 PostgreSQL 动态条件 SQL 问题，重新冷构建 oa-app 后复测通过。

## 已修改文件

- `oa-iam/**`：角色、授权、ABAC、JIT、数据范围与对象权限核心实现及测试。
- `oa-security/**`：对象权限注解与数据范围检查端口。
- `oa-org/**`、`oa-flow/**`、`oa-doc/**`、`oa-admin-biz/**`、`oa-file-service/**`、`oa-notify-service/**`、`oa-report/**`：业务资源权限治理。
- `oa-console/src/pages/iam/**`：角色管理和授权页面及测试。
- `docs/**`、`README.md`：权限、架构、API、运行手册和交付文档。
- 各模块数据库迁移及 API/数据访问基线文件。

## 未完成

- 无。本次权限加固、文档、冷构建和本地部署目标已完成。

## 当前问题

- 无功能阻塞；代码提交状态以当前仓库 `git log` 和远程分支为准。
- 角色的 `default_scope` 当前同时作为授权时的默认范围和最大范围上限；已在文档明确，双层运营配置属于后续需求。
- 远程 GitHub Actions 未触发；本地 CI 等价门禁已经全部通过。

## 下一步建议

1. 如要启用真实多租户或拆分角色默认/最大范围，另立需求和迁移计划。

## 恢复 Prompt

请读取 `CODEX_PROGRESS.md`；本次任务已完成。若继续，请先审阅当前工作树与交付报告，再处理新的明确需求。
