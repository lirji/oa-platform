<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.lrj.oa</groupId>
    <artifactId>oa-platform</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>oa-notify-service</artifactId>
  <name>oa-notify-service</name>
  <description>★ 独立部署 :8401 —— WebSocket 长连 + 站内信 + 多渠道推送（按连接数扩容）</description>
  <dependencies>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-actuator</artifactId></dependency>
    <dependency><groupId>com.lrj.oa</groupId><artifactId>oa-common</artifactId></dependency>
    <dependency><groupId>com.lrj.oa</groupId><artifactId>oa-protocol</artifactId></dependency>
    <dependency><groupId>com.baomidou</groupId><artifactId>mybatis-plus-spring-boot3-starter</artifactId></dependency>
    <dependency><groupId>org.postgresql</groupId><artifactId>postgresql</artifactId><scope>runtime</scope></dependency>
    <dependency><groupId>org.flywaydb</groupId><artifactId>flyway-core</artifactId></dependency>
    <dependency><groupId>org.flywaydb</groupId><artifactId>flyway-database-postgresql</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-data-redis</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-websocket</artifactId></dependency>
    <dependency><groupId>org.springframework.kafka</groupId><artifactId>spring-kafka</artifactId></dependency>
    <dependency><groupId>org.roaringbitmap</groupId><artifactId>RoaringBitmap</artifactId></dependency>
    <dependency><groupId>com.lrj.oa</groupId><artifactId>oa-security</artifactId></dependency>
    <!--
      ★ 判权引擎必须【嵌在每个可部署单元里】。
      @RequiresPerm 的切面与 fail-closed 守卫都在 oa-iam；只依赖 oa-security 的话，
      注解能编译、能读、但没有任何人执行它 —— 一个纯装饰的安全注解比没有更危险。
      改成 HTTP 问 oa-app 也不行：判权在热路径上，契约是零远程调用、P99 < 1ms。
      oa-iam 的三级缓存本就是"每节点一份 L1 + 共享 L2 + 失效总线"的设计，
      多一个节点正是它预期的形态。
    -->
    <dependency><groupId>com.lrj.oa</groupId><artifactId>oa-iam</artifactId></dependency>
    <dependency>
      <groupId>com.lrj.oa</groupId><artifactId>oa-security</artifactId>
      <version>${project.version}</version><type>test-jar</type><scope>test</scope>
    </dependency>
    <dependency><groupId>com.tngtech.archunit</groupId><artifactId>archunit-junit5</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <executions><execution><goals><goal>repackage</goal></goals></execution></executions>
      </plugin>
    </plugins>
  </build>
</project>
